# FLARE Website V2

A rebuilt static FLARE homepage concept using the existing brand direction and real FLARE event photography.

## Files
- `index.html` — new homepage
- `styles.css` — full visual system
- `script.js` — GSAP/ScrollTrigger/Lenis motion and interactions
- `assets/` — supplied FLARE visuals

## Notes
- The existing repository is not modified by this package.
- Replace the founder image URL with a local `bivek-ray.jpeg` if desired.
- Update `hello@flareevents.in` if the official contact email differs.
- The CDN libraries can later be self-hosted for maximum performance.
